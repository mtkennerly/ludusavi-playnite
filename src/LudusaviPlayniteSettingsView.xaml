<UserControl x:Class="LudusaviPlaynite.LudusaviPlayniteSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel>
        <StackPanel Margin="10">
            <TextBlock Text="{Binding ExecutablePath_Label}" />
            <TextBox Text="{Binding ExecutablePath}" />
        </StackPanel>

        <StackPanel Margin="10">
            <TextBlock Text="{Binding BackupPath_Label}" />
            <TextBox Text="{Binding BackupPath}" />
        </StackPanel>

        <CheckBox Margin="10" IsChecked="{Binding DoBackupOnGameStopped}" Content="{Binding DoBackupOnGameStopped_Label}" />

        <CheckBox Margin="10" IsChecked="{Binding AskBackupOnGameStopped}" Content="{Binding AskBackupOnGameStopped_Label}" />
    </StackPanel>
</UserControl>
